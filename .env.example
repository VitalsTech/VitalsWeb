# Base URL of the Vitals API Gateway (see contract.txt / OpenAPI spec).
# Leave empty to call same-origin relative paths (/api/v1/...), e.g. when
# running behind a reverse proxy that forwards /api to the gateway.
VITE_API_BASE_URL=http://localhost:5080
